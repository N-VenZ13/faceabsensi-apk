<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style
        name="Theme.Faceabsensi" parent="android:Theme.Material.Light.NoActionBar"

        >
        <!-- Tambahkan 2 baris ini untuk membuat background awal putih dan mematikan ikon default jika memungkinkan -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
    </style>
</resources>